import bossStats from "../../data/else/bossStats.json";
import prizeIcon from "../../data/else/prizeIcon.json";

export const rfabParser = (allMarkers) => {
	console.log(allMarkers, bossStats, prizeIcon);
};
